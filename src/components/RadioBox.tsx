import React, { useState } from "react";

interface radioBoxProps {
  optionsList: string[];
  name: string;
  onChange: () => void;
  value: string[];
}

function RadioBox({ optionsList, name, onChange, value }: radioBoxProps) {
  return <></>;
}

export default RadioBox;
